---
kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: snmp-trapper-debugger
  name: snmp-trapper-debugger
spec:
  ports:
  - name: app
    port: {{ trapper_debugger_port }}
    targetPort: {{ trapper_debugger_port }}
    protocol: UDP
  selector:
    k8s-app: snmp-trapper-debugger